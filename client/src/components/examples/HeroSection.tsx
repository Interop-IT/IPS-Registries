import { HeroSection } from '../HeroSection';

export default function HeroSectionExample() {
  return (
    <HeroSection
      totalVendors={12}
      totalResults={18}
      latestEvent="North America 2025"
    />
  );
}
